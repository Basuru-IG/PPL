
    class Drawing {
        const CANVAS_WIDTH: int = 800;
        const CANVAS_HEIGHT: int = 600;
        
        func drawRectangle(x: float, y: float, width: float, height: float): void {
            // Drawing a rectangle
        }

        func drawCircle(x: float, y: float, radius: float): void {
            // Drawing a circle
        }

        func drawText(x: float, y: float, text: string): void {
            // Drawing text
        }

        func clearCanvas(): void {
            // Clearing the canvas
        }
    }
    